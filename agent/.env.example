# Codex CLI backend (no API keys)
#
# Prereq: you are logged into Codex CLI
#   codex login

# Optional overrides
CODEX_BIN=codex
CODEX_MODEL=
CODEX_TIMEOUT_SECONDS=120

# Note: this repo always invokes Codex with:
#   --dangerously-bypass-approvals-and-sandbox
#   --search

# Optional budget tracking (approximate; Codex CLI does not expose cost)
LLM_COST_PER_1K_TOKENS_USD=
LLM_TOKENS_PER_CHAR=0.25

# Safety (legacy tools like agent/tools/code_review.py also respect this env var)
AGENT_UNSAFE_MODE=false

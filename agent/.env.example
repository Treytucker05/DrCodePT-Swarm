# DrCodePT-Swarm Configuration

# Ollama
OLLAMA_PRIMARY_MODEL=qwen2.5:7b-instruct
OLLAMA_FALLBACK_MODEL=gemma3:4b

# Browser (set to "false" to see browser window)
BROWSER_HEADLESS=true

# Self-Healing threshold (0.0-1.0)
SELF_HEAL_CONFIDENCE=0.7

# Codex timeout in seconds
CODEX_TIMEOUT_SECONDS=120

# OpenAI-compatible API (optional)
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Claude (used by `agent/agent_planner.py` + advanced launcher)
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-haiku-4-5

# Optional: override credential-store key (otherwise stored in user profile)
AGENT_CREDENTIAL_KEY=

# Optional notifications (Pushover)
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=

# DrCodePT Agent (keyless defaults)

# Browser/Vision LLM: not required (deterministic executor)
# Leave blank or point to a local OpenAI-compatible server if desired.
OPENAI_API_KEY=
OPENAI_BASE_URL=

# Browser settings
BROWSER_USE_HEADLESS=true

# Optional credentials (use ${ENV_VAR} in tasks instead of literals)
BLACKBOARD_USERNAME=
BLACKBOARD_PASSWORD=

# Notifications (Pushover optional)
PUSHOVER_USER_KEY=
PUSHOVER_API_TOKEN=

# Anki (optional)
ANKI_USERNAME=
ANKI_PASSWORD=

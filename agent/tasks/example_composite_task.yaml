id: example_composite_task
name: Example Composite Task
type: composite
goal: "Create a small file and verify it"
inputs: {}
output: {}
definition_of_done: "File created and verified"
verify: []
allowed_paths:
  - runs/
tools_allowed:
  - composite
stop_rules:
  max_attempts: 1
  max_minutes: 5
  max_tool_calls: 10
on_fail: abort
steps:
  - id: step_create_file
    name: Create file
    type: fs
    goal: "Write runs/example.txt"
    inputs:
      path: runs/example.txt
      content: "hello"
      mode: overwrite
    output: {}
    definition_of_done: "File written"
    verify:
      - id: file_exists
        args:
          path: runs/example.txt
    allowed_paths:
      - runs/
    tools_allowed:
      - fs
    stop_rules:
      max_attempts: 1
      max_minutes: 2
      max_tool_calls: 2
    on_fail: abort


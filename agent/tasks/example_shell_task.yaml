id: example_shell_task
name: Example Shell Task
type: shell
goal: "Print hello from PowerShell"
inputs: {}
output: {}
definition_of_done: "Command exits 0"
verify:
  - id: command_exit_zero
    args: {}
allowed_paths:
  - runs/
tools_allowed:
  - shell
stop_rules:
  max_attempts: 1
  max_minutes: 2
  max_tool_calls: 2
on_fail: abort
command: |
  Write-Output "hello from shell"
  exit 0


id: test_shell_001
name: Test Shell Tool
type: shell
goal: Run echo command and verify it succeeds
inputs:
  message: "Hello from agent"
output:
  destination: stdout
  format: text
definition_of_done: "Echo command exits with code 0 and outputs the message"
verify:
  - id: command_exit_zero
    args: {}
allowed_paths: []
tools_allowed:
  - shell
stop_rules:
  max_attempts: 2
  max_minutes: 1
  max_tool_calls: 5
on_fail: abort
command: echo "Hello from agent"
